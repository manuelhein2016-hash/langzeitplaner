{
  "name": "langzeitplaner",
  "version": "1.0.0",
  "private": true,
  "description": "Rolling 12-month whiteboard calendar for macOS — Jahresplaner poster, digitised.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/tier1/*.test.js\"",
    "test:watch": "node --test --watch \"tests/tier1/*.test.js\"",
    "test:dom": "./tests/run-dom-tests.sh",
    "test:all": "npm test && npm run test:dom",
    "dev": "node dev-server.mjs",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
